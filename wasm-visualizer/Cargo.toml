[package]
name = "qrap-wasm-visualizer"
version = "0.1.0"
edition = "2021"
authors = ["QRAP Team"]
description = "WebAssembly visualizer for QRAP quantum simulation"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console"] }
qrap-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde-wasm-bindgen = "0.6"

[dev-dependencies]
wasm-bindgen-test = "0.3"
